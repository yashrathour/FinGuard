# FinGuard
A fraud detection web application using machine learning models like XGBoost and Autoencoder trained on paysim1 dataset which have over 6 Million+ transactions, to identify fraud transactions in your dataset, go to website and upload your dataset in form of csv(make sure that dataset follows the structure of paysim1 for results)
